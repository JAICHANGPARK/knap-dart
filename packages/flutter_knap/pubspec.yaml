name: flutter_knap
description: Flutter widgets and real-time preview tools for the Knap template engine.
version: 2026.9.26
repository: https://github.com/JAICHANGPARK/knap-dart
issue_tracker: https://github.com/JAICHANGPARK/knap-dart/issues
resolution: workspace

topics:
  - markdown
  - template
  - flutter-widgets
  - obsidian
  - preview

environment:
  sdk: ^3.5.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_markdown: ^0.7.3
  knap: ^2026.9.26

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter
